int year;
            scanf("%d", &year);
            if (year != 0) {
                db->albums[i].releaseYear = year;
            }
            
            getchar(); // очистка буфера
            
            printf("Введите новый стиль (оставьте пустым для пропуска): ");
            fgets(input, sizeof(input), stdin);
            input[strcspn(input, "\n")] = '\0';
            if (strlen(input) > 0) {
                strcpy(db->albums[i].style, input);
            }
            
            printf("Введите новое количество треков (-1 для пропуска): ");
            int tracks;
            scanf("%d", &tracks);
            if (tracks != -1) {
                db->albums[i].trackCount = tracks;
            }
            
            printf("Введите новую продолжительность (-1 для пропуска): ");
            int duration;
            scanf("%d", &duration);
            if (duration != -1) {
                db->albums[i].durationMinutes = duration;
            }
            
            printf("Альбом обновлен!\n");
            return;
        }
    }
    printf("Альбом не найден!\n");
}

void displayAllAlbums(const Database *db) {
    if (db->count == 0) {
        printf("База данных пуста!\n");
        return;
    }
    
    printf("\n=== ВСЕ АЛЬБОМЫ (%d) ===\n", db->count);
    for (int i = 0; i < db->count; i++) {
        printf("\nАльбом #%d:\n", i + 1);
        printAlbum(&db->albums[i]);
    }
}

void filterAlbums(const Database *db) {
    if (db->count == 0) {
        printf("База данных пуста!\n");
        return;
    }
    
    printf("\n=== ФИЛЬТР АЛЬБОМОВ ===\n");
    printf("Введите минимальное количество треков: ");
    int minTracks;
    scanf("%d", &minTracks);
    
    printf("Введите максимальную продолжительность (минут): ");
    int maxDuration;
    scanf("%d", &maxDuration);
    
    printf("\nАльбомы, соответствующие условию (треков >= %d, длительность <= %d мин):\n",
           minTracks, maxDuration);
    
    int found = 0;
    for (int i = 0; i < db->count; i++) {
        if (db->albums[i].trackCount >= minTracks && 
            db->albums[i].durationMinutes <= maxDuration) {
            printAlbum(&db->albums[i]);
            found = 1;
        }
    }
    
    if (!found) {
        printf("Нет альбомов, соответствующих условиям.\n");
    }
}

// Функция сравнения для сортировки
int compareByYear(const void *a, const void *b) {
    return ((MusicAlbum*)a)->releaseYear - ((MusicAlbum*)b)->releaseYear;
}

void sortByYear(Database *db) {
    qsort(db->albums, db->count, sizeof(MusicAlbum), compareByYear);
}

void printAlbum(const MusicAlbum *album) {
    printf("┌─────────────────────────────────────┐\n");
    printf("│ Название: %-25s │\n", album->title);
    printf("│ Год выпуска: %-22d │\n", album->releaseYear);
    printf("│ Стиль: %-28s │\n", album->style);
    printf("│ Количество треков: %-18d │\n", album->trackCount);
    printf("│ Продолжительность: %-17d мин │\n", album->durationMinutes);
    printf("└─────────────────────────────────────┘\n");
}

4. src/fileio.h
c
#ifndef FILEIO_H
#define FILEIO_H

#include "database.h"

// Сохранение базы данных в файл
int saveToFile(const Database *db, const char *filename);

// Загрузка базы данных из файла
int loadFromFile(Database *db, const char *filename);

#endif

5. src/fileio.c
c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "fileio.h"

int saveToFile(const Database *db, const char *filename) {
    FILE *file = fopen(filename, "wb");
    if (file == NULL) {
        printf("Ошибка открытия файла для записи!\n");
        return 0;
    }
    
    // Сохраняем количество записей
    fwrite(&db->count, sizeof(int), 1, file);
    
    // Сохраняем все альбомы
    for (int i = 0; i < db->count; i++) {
        fwrite(&db->albums[i], sizeof(MusicAlbum), 1, file);
    }
    
    fclose(file);
    printf("Данные сохранены в файл '%s' (%d записей)\n", filename, db->count);
    return 1;
}
int loadFromFile(Database *db, const char *filename) {
    FILE *file = fopen(filename, "rb");
    if (file == NULL) {
        printf("Ошибка открытия файла для чтения!\n");
        return 0;
    }
    
    // Читаем количество записей
    fread(&db->count, sizeof(int), 1, file);
    
    // Проверяем на переполнение
    if (db->count > MAX_ALBUMS) {
        printf("Файл содержит больше записей, чем может вместить база данных!\n");
        fclose(file);
        return 0;
    }
    
    // Читаем все альбомы
    for (int i = 0; i < db->count; i++) {
        fread(&db->albums[i], sizeof(MusicAlbum), 1, file);
    }
    
    fclose(file);
    printf("Данные загружены из файла '%s' (%d записей)\n", filename, db->count);
    return 1;
}

6. src/utils.h
c
#ifndef UTILS_H
#define UTILS_H

// Очистка буфера ввода
void clearInputBuffer(void);

#endif

7. src/utils.c
c
#include <stdio.h>

void clearInputBuffer(void) {
    int c;
    while ((c = getchar()) != '\n' && c != EOF);
}

8. Makefile
makefile
CC = gcc
CFLAGS = -std=c99 -Wall -Wextra -I./src
TARGET = music_db
SRC_DIR = src
OBJ_DIR = obj

SOURCES = $(SRC_DIR)/main.c $(SRC_DIR)/database.c $(SRC_DIR)/fileio.c $(SRC_DIR)/utils.c
OBJECTS = $(SOURCES:$(SRC_DIR)/%.c=$(OBJ_DIR)/%.o)

all: $(TARGET)

$(TARGET): $(OBJECTS)
 $(CC) $(CFLAGS) -o $@ $^

$(OBJ_DIR)/%.o: $(SRC_DIR)/%.c | $(OBJ_DIR)
 $(CC) $(CFLAGS) -c $< -o $@

$(OBJ_DIR):
 mkdir -p $(OBJ_DIR)

clean:
 rm -rf $(OBJ_DIR) $(TARGET)

run: $(TARGET)
 ./$(TARGET)

.PHONY: all clean run

9. README.md
markdown
# База данных музыкальных альбомов 🎵

Консольное приложение для создания, хранения и управления информацией о музыкальных альбомах.

---

## 🎯 Назначение

Проект демонстрирует практическое применение структур данных, файлового ввода-вывода и алгоритмов поиска/сортировки в языке C. Может использоваться как учебный пример для изучения основ программирования на C или как основа для персональной коллекции музыкальных альбомов.

---

## 📋 Функциональные возможности

### Основной функционал:

- ✅ CRUD-операции: Создание, чтение, обновление, удаление записей об альбомах
- ✅ Поиск: По названию альбома (частичное совпадение)
- ✅ Фильтрация: По количеству треков и продолжительности
- ✅ Сортировка: По году выпуска (возрастание)
- ✅ Сохранение: Бинарный формат для долговременного хранения
- ✅ Консольный интерфейс: Интерактивное меню с 10 командами

### Структура данных альбома:

| Поле | Тип | Описание |
|------|-----|----------|
| title | char[100] | Название альбома |
| releaseYear | int | Год выпуска |
| style | char[50] | Музыкальный стиль |
| trackCount | int | Количество треков |
| durationMinutes | int | Продолжительность в минутах |

---

## 🏗️ Архитектура проекта

### Модульная структура:


src/
├── main.c          # Точка входа, управление меню
├── database.h/c    # Модель данных и операции с базой
├── fileio.h/c      # Работа с файлами (сохранение/загрузка)
└── utils.h/c       # Вспомогательные функции
### Ключевые алгоритмы:

1. Поиск: Линейный поиск по полю title с частичным совпадением (strstr)
2. Сортировка: Быстрая сортировка через qsort() по полю releaseYear
3. Фильтрация: Условия по минимальному количеству треков и максимальной продолжительности
4. Управление памятью: Статический массив фиксированного размера (100 записей)

---

## 🚀 Сборка и запуск

### Требования:

- Компилятор C (GCC/Clang/MSVC)
- Стандарт C99 или новее
- Система сборки Make (опционально)

### Инструкция по сборке:

#### Способ 1: С использованием Makefile

Bash

# 1. Клонирование репозитория (если используется Git)
git clone [URL-репозитория]

# 2. Переход в директорию проекта
cd music-albums-db

# 3. Сборка проекта
make

# 4. Запуск
make run
# или
./music_db
Способ 2: Прямая компиляция

Bash

gcc -std=c99 -o music_db src/*.c
./music_db
Использование:
1. Запустите программу
2. При первом запуске вы можете загрузить тестовые данные или начать с пустой базы
3. Следуйте инструкциям в консольном меню
4. Для выхода выберите пункт 0
5. Важно: Сохраните данные перед выходом (пункт 8 в меню)

---

🤝 Вклад в проект

Проект является учебным и открыт для улучшений:

· Сообщения об ошибках: через Issues с подробным описанием
· Предложения по улучшению: новые функции, улучшение интерфейса
· Требования к коду: соблюдение стиля, комментарии к сложным участкам

Планируемые улучшения:

· Добавление поиска по диапазону годов
· Поддержка экспорта в текстовый формат (CSV)
· Улучшение валидации вводимых данных
· Добавление статистики (средняя продолжительность, популярные стили)
· Поддержка разных критериев сортировки
· Добавление цветового оформления консоли

---

Автор: [Ваше имя и группа]
Курс: Основы программирования на C
Год: 2024
Лицензия: MIT

Проект создан в образовательных целях
